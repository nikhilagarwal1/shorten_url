<%= form_with(model: shortener, local: true) do |form| %>
  <% if shortener.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shortener.errors.count, "error") %> prohibited this shortener from being saved:</h2>

      <ul>
      <% shortener.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :shortener_url %>
  </div>

  <!-- <div class="field">
    <%= form.label :shorten_url %>
    <%= form.text_field :shorten_url, id: :shortener_shorten_url %>
  </div> -->

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
